# Done

Closed tasks. Format: `- [x] <task> @done(<isoTimestamp>) #git:<first8ofcommitId>`.

- `@done(...)` — ISO 8601 timestamp the task was closed.
- `#git:<first8ofcommitId>` — first 8 chars of the commit that completed the task. Use `#git:pending` until committed, then backfill with the real short SHA.

Tasks land here (or in [backlog.md](./backlog.md)) **before** being deleted from the live task list — a PreToolUse hook blocks `TaskUpdate status:deleted` otherwise. See [CLAUDE.md](../../CLAUDE.md).

- [x] Rescue the two genuinely-new draft posts from the stale `feat/usecase-diagrams` branch (3 weeks old, 7 commits, no PR). Investigation: the branch heavily overlapped work already merged to main by another route (the UseCaseDiagram engine, earlbear-use-cases.mdx, concurrent-commit skill, features docs — all on main in newer/divergent form; 11 merge conflicts). Only 2 files were truly new: `from-one-laptop-to-the-cloud.md` (design: local-cloud-arch) and `what-we-never-collect.md` (design: collection-sync). Cherry-picked just those onto rescue/telemetry-draft-posts off current main; added the missing `audience: internal` (posts predate the audience split; classified internal per their architecture depth + internal design-doc back-refs, matching the earlbear-use-cases sibling). Both stay draft:true. Verified: posts-check, design-check (slugs registered), full build, audience guard (both absent from external dist), make check. @done(2026-07-23T00:00:00Z) #git:18fff8d

- [x] Fix stale `tasks-check` / `make check`: package.json invoked the deleted `.claude/hooks/check-task-files.py` (removed in ef0062b when task-tracking moved to the task-management-manager plugin). Repointed `tasks-check` to `scripts/tasks-check.sh`, which resolves the plugin's shipped `check-task-files.py --check` (marketplace source, then plugin cache) and skips gracefully if the plugin is absent. `make check` green again. @done(2026-07-20T00:00:00Z) #git:990c531

- [x] Build the import-design-post skill + 7 new zero-dep Astro components (Callout, Assumption, Mockup, interactive Walkthrough, DecisionTable, SequenceDiagram, DataModel) to import rich design posts from the sibling bytesofpurpose Docusaurus blog, preserving the whole experience (walkthroughs, mockups, decision tables, use-case/flow/sequence/ER diagrams). Added catalog rows for the 3 new diagram primitives + a docs/features why-doc. Imported both source posts as TWO authored flavors each: self-healing-storefront + ecommerce-site-scanner as external (100k-ft, audience external, secret-sauce-softened) and *-design as internal (as-is, full fidelity). All hand-authored, de-em-dashed, EarlBear voice; illustrative figures footnoted as derived. Verified: posts-check, diagrams-check, build, in-browser render of every diagram, and audience-guard (both internal posts absent from the external dist, both external present). @done(2026-07-09T09:00:00Z) #git:0ce95f1

- [x] Scaffold Astro blog: config, content model, layouts, components, pages, seed post @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Vendor design-system CSS + assets into the repo (GitHub Packages downloads billing-blocked) @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Verify dev server and production build (all routes, Shiki, RSS, favicon) @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Add "track your work" task convention to CLAUDE.md + non-blocking nudge hook @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Build docs/features/ "why" system: content-hash cache, rename-proof drift hook, features:check, feature-docs skill @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Add Makefile with targets (dev/build/preview/deploy/sync-assets/regen-favicon/checks) @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Track A: build UseCaseDiagram component (build-time inline-SVG UML use-case diagrams — Earl-mark actor, oval use cases, gradients, ambient drift + draw-in animation, reduced-motion + mobile-safe) + proof post `earlbear-use-cases.mdx` + feature doc; add `@astrojs/mdx`, widen content glob + check-posts.py to `.mdx` @done(2026-07-04T00:00:00Z) #git:6fce5aa7
- [x] Track C: build diagram perf + A/B harness (`scripts/diagram-bench.mjs`, `make bench-diagram`, dev-only `src/pages/bench/diagram.astro`) — measures animated/static/image variants on desktop+mobile via headless-Chrome CDP (doc weight, FCP, CLS, long-frames) → `docs/diagram-bench.md`; evidence cited in the feature doc + proof post @done(2026-07-04T00:00:00Z) #git:6fce5aa7
- [x] Track B: build `frontend-audit` skill (local + advisory headless-Chrome audit: perf/a11y/SEO/bundle/mobile; two-tier fixes applied only on approval; governs when to A/B test + how to gather and document evidence) + wire into CLAUDE.md skills list; add evidence-citation guidance to new-post skill @done(2026-07-04T00:00:00Z) #git:6fce5aa7
- [x] Concurrent-session safety: post-commit auto-capture of other sessions' WIP (`scripts/concurrent-capture.sh` + `.githooks/post-commit`, backup/<ts> tags), pre-rebase guard against history rewrites, pull.rebase/autoStash/rerere replay config (`scripts/setup-concurrency.sh`), `concurrent-commit` skill + recovery playbook, CLAUDE.md concurrency rules @done(2026-07-04T00:00:00Z) #git:cbf7ba3d
- [x] UseCaseDiagram layout quality: two-sided actors (internal/system left, external right), overlap-minimizing use-case order, barycenter actor placement, blocking build-time overlap gate (≥75% crossing-free) + actor line-angle balance gate, `allowOverlap` escape hatch @done(2026-07-04T00:00:00Z) #git:e0c0e4c1
- [x] UseCaseDiagram click-to-focus detail modal (progressive-enhancement native `<dialog>`; use-case detail + actors + include/extend; zero-JS fallback); `detail` field on UseCase; proof-post use cases filled in @done(2026-07-04T00:00:00Z) #git:deb29e30
- [x] Create `usecase-diagram` authoring skill (spec, two-sided layout, overlap-min ordering, the two blocking gates, the modal) + wire into CLAUDE.md; update feature doc @done(2026-07-04T00:00:00Z) #git:9fad7dec
- [x] Build FlowDiagram primitive (pipeline/loop/sequence): zero-dep build-time inline SVG, gradient nodes, edge/store/external node kinds, blocking overlap gate, draw-in animation, click-to-focus modal, auto-loop from back-edge @done(2026-07-04T00:00:00Z) #git:a10de26
- [x] Build "our extended Mermaid": zero-dep parser (mermaid-parse.ts) for the Mermaid flowchart subset → FlowDiagram; verified no pure-JS Mermaid renderer works here (getBBox on jsdom/svgdom fails on Node 20+22), so we parse not render — no browser, no dep @done(2026-07-04T00:00:00Z) #git:a10de26
- [x] Build Accordion foldable-options component (native details/summary, zero JS, verdict pills) for decision posts @done(2026-07-04T00:00:00Z) #git:a10de26
- [x] Enrich 4 proof posts (→.mdx): agentic-workflow (loop), mining-transcripts (pipeline w/ edge boundary), from-one-laptop (architecture), one-database-two-modes (accordion) @done(2026-07-04T00:00:00Z) #git:a10de26
- [x] Build enrich-post skill + living catalog.md (technique table + Mermaid decision rule) + wire into CLAUDE.md; fix new-post dangling dataviz ref; diagram-catalog feature doc @done(2026-07-04T00:00:00Z) #git:a10de26
- [x] Enrich via /enrich-post: syncing-append-only-transcripts (rewind-guard decision flow, FlowDiagram sequence) + what-we-never-collect (raw-stays trust-boundary pipeline); note branch-layout gap in catalog.md @done(2026-07-04T00:00:00Z) #git:38a012f6
- [x] Add FlowDiagram shape="branch": layered top-down layout that fans a fork's outcomes into a row with labeled edges (passes overlap gate, no allowOverlap); re-render syncing rewind-guard as a branch; close the catalog gap @done(2026-07-04T00:00:00Z) #git:e3df97e5
- [x] Enrich a-pitch-deck-that-reads-its-own-numbers (→.mdx): "one selector, two surfaces" branch — marts → shared selector → deck + dashboard @done(2026-07-04T00:00:00Z) #git:e3df97e5
- [x] Auto-detect FlowDiagram shape from graph (loop from back-edge, branch from a 2+-out-degree fork) for both native + Mermaid paths, so Mermaid forks render as branches with no shape prop; removed now-unneeded allowOverlap on from-one-laptop @done(2026-07-04T00:00:00Z) #git:1c5460f9
- [x] Fix missing FlowDiagram import in from-one-laptop-to-the-cloud.mdx (rendered fine in prod because draft, broke in dev) @done(2026-07-04T00:00:00Z) #git:1c5460f9
- [x] Add check-diagrams.py (npm run diagrams-check, make check, PostToolUse hook): every Capitalized component used in an .mdx post must be imported — catches the missing-import class of bug that drafts hide from the build @done(2026-07-04T00:00:00Z) #git:1c5460f9
- [x] Add 4 build-time diagram-quality gates to FlowDiagram + UseCaseDiagram: dangling-reference (throws), plus warnings for missing desc (a11y), disconnected node/actor, and over-long label; documented in catalog.md @done(2026-07-04T00:00:00Z) #git:dad9c263
- [x] Add optional `legend` to FlowDiagram: renders a key (swatch + meaning) for the store/external/edge node kinds actually used; enabled on the mining pipeline post; documented in catalog + enrich-post skill @done(2026-07-04T00:00:00Z) #git:512a352b
- [x] Add `legend` to the remaining FlowDiagram posts that use special node kinds (from-one-laptop, syncing, what-we-never-collect, pitch-deck) so every diagram with edge/store/external boxes explains them @done(2026-07-04T00:00:00Z) #git:6862a787
- [x] Add FlowDiagram shape="swimlane": nodes carry a `lane`; each lane is a labeled horizontal band, flow reads left→right by longest-path rank, cross-lane edges show handoffs; documented in catalog @done(2026-07-05T00:00:00Z) #git:b9e4f43b
- [x] Add visual-density nudge (check-visual-density.py, npm run visuals-check, make check, advisory PostToolUse hook): warns when an H2 section runs > 280 prose words with no visual; capture the 'picture is worth a thousand words' tenet in CLAUDE.md brand-voice + new-post skill + the warning text @done(2026-07-05T00:00:00Z) #git:3b88cbfe
- [x] Create `new-diagram-kind` skill (thinking process for adding a new diagram kind: survey the catalog first, match the house engine, prove it, keep the catalog living) + catalog-upkeep hook (check-diagram-catalog.py, npm run catalog-check, make check, PostToolUse) that flags a diagram component or FlowDiagram shape missing from catalog.md; wired into CLAUDE.md @done(2026-07-05T00:00:00Z) #git:33999b9f
- [x] Act on a visual-density nudge: add a swimlane to agentic-workflow 'The loop' (each agent a lane, artifacts handing off across lanes) — clears the nudge and gives swimlane its catalog example; judged the other 4 flagged sections as genuine prose (not forced) @done(2026-07-05T00:00:00Z) #git:f32dcddc
- [x] Fix swimlane width blowout (rank forward edges only, compress columns) + highlight FlowDiagram edge labels with a chip (surface rect sized from label length) @done(2026-07-05T00:00:00Z) #git:f3be4a8
- [x] Build ComparisonMatrix component (options × criteria, chosen column highlighted, yes/no/partial marks, a11y table, mobile-scroll) + catalog row; broaden catalog-check to match *Matrix components; wire into one-database-two-modes (matrix + accordion pair) @done(2026-07-05T00:00:00Z) #git:95c5cfca
- [x] Frontmatter-driven visual-expectation check: `expects: [comparison|flow|use-case|decision]` field in the zod schema; check-visual-expectations.py (npm run expects-check, make check, PostToolUse hook, advisory) warns when a declared visual's component is absent; declared expects:[comparison] on one-database; documented in new-post skill @done(2026-07-05T00:00:00Z) #git:dc2df544
- [x] Give expects-check teeth: declare `expects` on the 8 posts whose type owes a visual (use-case → earlbear-use-cases; flow → the 6 FlowDiagram posts; comparison → one-database); verified all pass + the check fires when a declared diagram is removed. Reconcile docs/tasks/backlog.md (remove already-built Track B/C, add deferred diagram ideas + Nav-overflow bug + infra items) @done(2026-07-05T00:00:00Z) #git:64726d67
- [x] Build skills: new-post, manage-authors, feature-docs, sync-design, deploy @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Regenerate favicon from earl-mark.svg (glasses mark), accent-tinted, via scripts/gen-favicon.mjs @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Multi-author support: authors collection, co-author bylines, /authors/ + /authors/<id>/, RSS author @done(2026-07-03T00:00:00Z) #git:71331877
- [x] Order authors index by explicit `order` field (Sa'd before Omar) @done(2026-07-03T00:00:00Z) #git:e50a090c
- [x] Replace outdated About illustration; distill landing narrative into About page, drop deprecated earl-analyst.svg @done(2026-07-03T00:00:00Z) #git:f2c79b52
- [x] [MANUAL] Cloudflare DNS: CNAME `blog` → `earlbear.github.io`, DNS only — verified resolving to GitHub Pages IPs 185.199.108-111.153 @done(2026-07-03T00:00:00Z) #git:c825b684
- [x] Push main + first deploy to gh-pages branch (branch published, ready for Pages source config) @done(2026-07-03T00:00:00Z) #git:57fee15c
- [x] Set up gitleaks secret-scanning hooks (.githooks pre-commit + pre-push, make install-hooks, .gitleaks.toml) — verified blocks a real PAT @done(2026-07-03T00:00:00Z) #git:1dadf345
- [x] Polish new-post skill (concrete verify commands, required questions field, body scaffold, build check) + posts-check hook @done(2026-07-03T00:00:00Z) #git:5c268695
- [x] Write "Life without EarlBear" post: growth-team roles, 2026 salary bands, A/B-test math, CSS gantt hiring timeline, interactive cost calculator (draft) @done(2026-07-03T17:00:00Z) #git:5dc35da2
- [x] Require MLA footnote citations for dollar/percent figures: citation checks in check-posts.py (ref↔def match, MLA shape on URL definitions, uncited-figure detection) @done(2026-07-03T17:00:00Z) #git:5dc35da2
- [x] new-post skill: infer author from machine username (AskUserQuestion fallback), websearch-verify claims, citation rules, widget/chart guidance, hook-failure recovery @done(2026-07-03T17:00:00Z) #git:5dc35da2
- [x] Investigate per-kind post frontmatter: single zod schema today; z.discriminatedUnion on a `kind` field is the supported path when a second post type appears @done(2026-07-03T17:00:00Z) #git:5dc35da2
- [x] Add support/operations engineer as sixth role (roles chart, gantt, calculator); recompute defaults to $102k/mo @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] Split RACI out of the roles table into its own badge grid (distinct R/A/C/I badges, legend, scrollable) @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] Replace salary text ranges with a bar+median-dot chart (shared axis, right-aligned value column, mobile stack) @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] Add researched top-of-market talent premium (60–85% over median, cited) + talent-tier slider to calculator @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] check-posts.py: detect footnote refs trapped inside raw-HTML blocks (they render literally) @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] Add global .prose table styling (cell padding, header rule, row separators, horizontal scroll) — fixes column crowding site-wide @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] new-post skill: tables-vs-HTML-components guidance, footnotes-in-raw-HTML caveat, verify-the-render (visual-review) step @done(2026-07-03T18:00:00Z) #git:5dc35da2
- [x] Investigate failed Pages deploy: auto pages-build-deployment deploy step failed server-side ("try again later"); site still served prior build @done(2026-07-03T19:00:00Z) #git:pending
- [x] Order post byline + RSS authors by author `order` field (then name) to match the authors index, not post frontmatter order @done(2026-07-03T19:00:00Z) #git:c6db727b
- [x] Harden deploy skill: watch pages-build-deployment run to completion via gh CLI, verify NEW content (not site-up), document gh debugging + deploy-failure recovery @done(2026-07-03T19:00:00Z) #git:1a8f5211
- [x] Internal vs external audience split: `audience: internal` schema field, allowlist filter in posts.ts (fail-closed external), PUBLIC_AUDIENCE per-target build (site/theme/marker), two deploy targets (gh-pages external + CF Pages internal), check-audience guard hook (frontmatter + dist-leak gate + env-target assert), domains.yaml record in earlbear-domain, docs + feature why-doc @done(2026-07-05T08:45:00Z) #git:pending
- [x] Make `audience` a required explicit frontmatter field (option A): removed the zod default so a missing audience FAILS the build; check-audience hook + --check now require the field (block on missing, not just on typo); backfilled `audience: external` on all 11 existing external posts; updated CLAUDE.md + audience-split feature doc. Closes the "forgot to mark → silently public" hole. @done(2026-07-05T09:07:00Z) #git:pending
- [x] Align internal deploy auth with the earlbear-domain deploy-contract audit (make audit-deploy): deploy:internal now uses a scoped dotenvx-encrypted CLOUDFLARE_API_TOKEN via `dotenvx run --` (not machine wrangler OAuth), calls ./node_modules/.bin/wrangler, and `deploy-internal`/`build-internal` gained a `node_modules: package-lock.json` (npm ci) prerequisite so a fresh checkout deploys. Added dotenvx devDep, .env.example, scripts/lastpass-key.sh + make key-backup/restore/status (LastPass = source of truth for .env.keys), .gitignore .env.keys, .gitleaks allowlist for encrypted ciphertext. blog.internal now passes all deploy-contract checks (auth INFO → green once token is set). @done(2026-07-05T12:54:00Z) #git:pending
- [x] Build audience-audit skill + check-audience-fit.py (advisory content-vs-declared-audience check): flags internal-sounding posts marked external (disclosure risk) via weighted signals; audited all posts → all correctly classified, nothing to move. Committed new files only to avoid sweeping in the concurrent session's in-flight audience work @done(2026-07-05T00:00:00Z) #git:d3be4aa
- [x] Deploy skill made audience-aware: Step 0 uses AskUserQuestion to pick external / internal / both, documents the internal Cloudflare Pages path (dotenvx key + CF project prereqs, leak gate, CNAME strip, gated-site verify), and the external gh-pages path as before @done(2026-07-05T00:00:00Z) #git:683bce84
- [x] Deploy BOTH sites: external (blog.earlbear.com, gh-pages) verified live — home+external 200, internal posts 404 (not public); internal (blog.internal.earlbear.com, first-ever Cloudflare Pages deploy) verified live + gated behind CF Access login. Fixed a deploy-script bug (PUBLIC_AUDIENCE must precede the assert-target guard) @done(2026-07-05T00:00:00Z) #git:7a13145d
- [x] Capture post-deployment validation in the deploy skill: external (internal-post-404 security check) + internal (CF-Access-gated check) live-verify steps, plus the deploy-script env-order note @done(2026-07-05T00:00:00Z) #git:fe9a36be
- [x] Fix ComparisonMatrix spacing: chosen-column highlight was a floating patch (light bg blended into the container) and the header baseline was uneven (chosen label+badge taller than single labels). Frame the chosen column with accent borders top-to-bottom so it reads as one defined column; stack label+badge in a flex inner + vertical-align middle so all option headers align @done(2026-07-06T00:00:00Z) #git:cf0b87ae
- [x] Investigate applying the ComparisonMatrix spacing fix to the "@earlbear/ui handbook port". Found the localhost:3000 handbook is served from a SEPARATE repo — `omars-lab.github.io/bytesofpurpose-blog` (a personal Docusaurus site that adapted the EarlBear diagram-kit, per its own git log), not the EarlBear blog or the `../earlbear-design-system` package (which exports no ComparisonMatrix). Its decision-kit is its own bundled component. Decision (user): leave that separate repo untouched — the EarlBear source fix is committed+pushed and the handbook picks it up on its next sync; no cross-repo edit here @done(2026-07-06T00:00:00Z) #git:n/a
- [x] Mirror the full earlbear-domain secret-sync flow into the blog repo: scripts/collect-secret.sh + make collect-secret/encrypt/decrypt (was only key-backup/restore/status). Setting/rotating the CF Pages token now follows the codified flow (make collect-secret VARS=CLOUDFLARE_API_TOKEN → make encrypt → make key-backup) matching ../earlbear-domain; updated .env.example + CLAUDE.md. Confirmed the token the other session set IS dotenvx-encrypted + injectable and the deploy-contract audit auth checks are green. @done(2026-07-06T12:25:00Z) #git:pending
- [x] Build external-post-review skill + check-secret-sauce.py (advisory hook): a section-level editorial pass on correctly-public posts that flags passages over-sharing proprietary "secret sauce" (verbatim prompts, full configs, exact tuning knobs, internal economics, recipe framing) and recommends softer rewrites that keep the marketing value — distinct from audience-audit (which moves whole posts). Wired npm run secret-sauce-check + PostToolUse hook (advisory, never blocks; silent on internal posts), added to CLAUDE.md Skills; calibrated (fires 6/6 on synthetic over-share, quiet on real storytelling posts). @done(2026-07-06T12:40:00Z) #git:pending
- [x] Change internal-only-example post pubDate 2026-07-05 → 2026-07-03 and redeploy the internal site so the new date is live @done(2026-07-06T00:00:00Z) #git:344128a3
- [x] Move the Repo Map from ../earlbear-sites into the internal blog as a standalone page. Vendored the artifact to src/repo-map/artifact.html (Mermaid diagram + click-through drawer, tokens inlined at build time from src/styles/tokens.css) and served it via src/pages/repo-map.astro — self-guarded to 404 on the external build (isInternalBuild, mirroring bench/diagram.astro). Closed the standalone-page audience gap: sitemap filter drops /repo-map/ on external, internal-only Nav link, and extended scan_dist() in check-audience.py to fail an external deploy that leaks /repo-map/. Moved the manage-repo-map skill + make repo-map-check into the blog (repointed at src/repo-map/artifact.html; 19/19 tests pass). Removed the artifact, skill, catalog.json entry, and Makefile target from earlbear-sites. Verified both builds + guard + interactive drawer in-browser. Also folded in four previously-missing repos (earlbear-blog, earlbear-agentic-workflow, earlbear-domain, earlbear-landing) with their edges, so make repo-map-check reports in sync. @done(2026-07-06T17:15:00Z) #git:ef3e1ce
- [x] First deploy: run `npm run deploy` to create/update the `gh-pages` branch — the external site is LIVE on blog.earlbear.com (HTTP 200; serves the published external post, flagship stays draft). External dist verified secret-clean (audience guard + gitleaks + no supabase/signing-key/CommentLayer/functions). @done(2026-07-13T00:00:00Z) #git:f4eb314
- [x] Internal blog one-time infra + deploy: the earlbear-blog-internal Cloudflare Pages project + blog.internal.earlbear.com + CF Access app (cc04d3ea) already existed (live since 2026-07-06); the comment-layer deploy (#100) set the 3 CF Pages secrets via `make wire-comment-secrets` and shipped the build + /api/auth-token Function via `make deploy-internal` (verified: 302→CF Access off-org, the correct gated default). @done(2026-07-13T00:00:00Z) #git:34962fd
- [x] Tighten `the-two-marts-that-werent-there.mdx` (internal, draft) from a live-investigation debugging journal into a crisp decision record: reshaped to problem → "looked like a security bug but wasn't" → what we shipped → why the review missed it; led with the shipped outcome (all marts `@earlbear.com`-gated + drift guard) instead of deliberating three open options; trimmed the ComparisonMatrix (6→5 criteria) and dropped the two now-redundant per-option FlowDiagrams; rewrote description + questions to past-tense decision-record framing; kept all repo specifics; stayed internal/draft. Verified: posts-check green (30 posts), internal build renders the post cleanly. Confirmed the post's claims are true+consistent — the drift guard exists (`../earlbear-agentic-workflow/scripts/check-mart-drift.mjs`), `marts-identity.spec.ts` backs the live proof, and the key-catalog cross-link already says "every mart is identity-gated". @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Re-center the two-marts post on the GATING use case (supersedes the prior decision-record reframe): retitled "The two marts that weren't there" → "Why every mart is identity-gated"; new spine = the rule (every mart @earlbear.com-gated) → why the DB needs its own gate (CF Access gates the page, not the API calls; the publishable key is replayable via curl; using(true) can't tell the difference) → why we could afford it (the two-build split: static build uses the secret key, live build a minted token, so neither needs anon-read) → how we found the gap (the missing-migration bug, demoted to evidence) → a "Things to watch out for" gotchas section (404≠403, silent snapshot fallback, advisor can't lint absent tables, new marts must reach prod, don't reintroduce using(true), static-key vs live-token symptom split); new "page gate is not the data gate" + "two builds, two keys" diagrams; trimmed ComparisonMatrix to anon-vs-gated after the split; rewrote questions to reader-facing. Verified: posts-check green, dev server serves the new title as internal (HTTP 200). Stayed internal/draft. @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Build the `reader-review` skill + `check-reader-flow.py` advisory hook: a reader-perspective narrative-comprehension pass on a post (distinct from design-post-review=visuals, external-post-review=over-sharing, audience-audit=classification). Skill (.claude/skills/reader-review/SKILL.md) does the seven story checks (cold open, undefined local terms, unexplained leap, missing on-ramp, narrative soundness, answers-its-own-questions, the landing) with a lane table and numbered process modeled on external-post-review. Paired hook is two-mode (npm run reader-check CLI + PostToolUse nudge on any touched post, always exit 0): flags mechanical tells (EarlBear-local term used before intro/link; declared question whose keywords never appear in body; a genuinely long single prose paragraph — calibrated to skip tables/lists so it doesn't dupe check-visual-density). Wired into package.json, .claude/settings.json (PostToolUse Write|Edit|MultiEdit), and CLAUDE.md Skills. Tested both modes; reader-check surfaces real signal (3 posts use "Cloudflare Access"/"anon key" cold). @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Blog-arc audit: ran reader-review across all 31 posts (6 parallel cold-reader agents by cluster + 1 arc-mapper) to find the on-ramp gap. Total convergence: NO post states what EarlBear is (product/business/stack) before diving in — every post is a dispatch from inside the system. Reader first falls off at from-one-laptop sentence 2 ("telemetry pipeline behind our agent dashboard"); the decoder ring arrives 2 posts later; the best stack-premise paragraph is buried in the-proxy-and-the-passport ¶1; "mart" (a title-noun) is defined only inside a diagram-node detail; the PUBLIC self-healing post never names the vendor and dangles an unlinkable "scanner" reference. About page covers product but names zero stack + no audience-split. Deliverable written to docs/blog-arc-audit.md (arc-vs-posts table, load-bearing undefined terms, per-post verdicts, recommendation). Recommendation: one foundational "Start here" post (5 sections + owns the term glossary) doubling as a reading-order index, linked from about.astro; then a light per-post pass adding one on-ramp link each. Feeds the write-foundational-post task. @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Wrote the foundational on-ramp post src/content/blog/start-here-the-whole-system.mdx (internal, draft) — "Start here: what EarlBear is, and the stack this blog is written from". Fixes the audit's root-cause gap. Five sections per the audit rec: what EarlBear is (surfaces the buried keystone fact "agents = Claude Code sessions → transcripts are the telemetry" into ¶2); what the blog is (zero-JS Astro, no backend → motivates the stack); the stack at a glance (one FlowDiagram: static site → Cloudflare Access → Supabase → RLS, each node linking its deep-dive, carrying the "Access is a door, RLS is the room" thesis + the mint-step bridge); two audiences + two-build split; a prose glossary defining mart/scan fleet/two-build split/snapshot builder (fixes "mart defined only in a diagram-node attr"); and an ordered reading list = arc stages 2→6. All 13 outbound links verified to resolve. Decisions (user): audience=internal, draft-the-post-only (per-post link wiring + public-post fix deferred). Verified: posts-check green (31 posts), renders internal HTTP 200, clean under its own reader-check hook. Independent cold-reader agent verdict: FOLLOWS (clears the on-ramp bar); its one soft-spot finding ("minted identity" used before the mint step was named) fixed by adding the mint-step bridge sentence + identity-post links. @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Wired on-ramp links into all 15 deep/context-assuming posts the audit flagged (from-one-laptop, one-database-two-modes, reporting-mcp-supabase-mode, every-key, pitch-deck, realtime-fleet, supabase-as-an-api, who-are-you-to-supabase, the-two-marts, comments-that-anchor, agents-that-report, consolidating-drive, mining-transcripts, earlbear-use-cases, one-engine-two-renderers). Used a uniform italic lead-in line — "*New here? [Start with the whole system](/blog/start-here-the-whole-system) — what EarlBear is and the stack this runs on.*" — placed before the first prose paragraph (or after the imports for the-two-marts, which opens on a heading). A link, not re-explanation, per the audit. Verified: posts-check green (31), full internal build clean (44 pages, no errors; only pre-existing usecase-diagram layout warnings on the untouched scanner posts), each post has the link exactly once, and reader-check confirms the fix works — realtime-fleet's undefined-"anon key" tell dropped now that it links the on-ramp early. @done(2026-07-13T00:00:00Z) #git:cb11edd
- [x] Fixed the PUBLIC self-healing-storefront.mdx on-ramp (the one external technical post). Named the vendor: the company was anonymous ("an operator (us)") — now "EarlBear" appears in the opening definition, the "who it is for" section, and the finding/fixing pairing, introduced at the moment the reader asks "whose agent is this?". Resolved the dangling scanner reference: "the second half of a pair / the first half is a scanner" pointed at nothing a public reader could reach — reworked to be self-contained ("EarlBear continuously scans a store for what is wrong — a slow checkout, a dead link, a weak product page — and this agent turns those findings into experiments"), no internal link. Also aligned the walkthrough mockup chrome (storefront-agent.app → EarlBear / earlbear.com) so the demo product surface matches the named vendor. All public info (the About page already states EarlBear scans + repairs). Verified: posts-check green, secret-sauce clean (no over-share), external build clean + audience-check confirms dist has no cross-audience content. Independent OUTSIDE-reader agent (no EarlBear knowledge): both holes CLOSED, verdict FOLLOWS. Post stays draft:external (held per the flagship keep-as-draft decision). @done(2026-07-13T00:00:00Z) #git:36409f6
- [x] internal-only-example.md: reframed from throwaway test fixture to a KEPT audience-split explainer (decision: keep, not delete). The audit found it's actually one of the blog's clearest "there are two blogs" explainers, and the new start-here post links it for exactly that. Retitled "Internal-only example post" → "How the audience split works"; rewrote the description; dropped the self-deprecating "you can delete this fixture" closer for a forward-looking "this post stays — the simplest live check that the split still holds" framing; added the standard on-ramp lead-in (which also cleared its reader-check "Cloudflare Access used cold" tell); repointed start-here's link text to the new title (slug/URL unchanged, so the link stayed valid). Caught + fixed a YAML parse error the build surfaced — the new description had an unquoted colon; wrapped it in quotes. Verified: posts-check green, both builds clean (internal renders it at 44 pages; external audience-guard confirms it stays out of the public dist), dev server serves the new title HTTP 200, start-here link resolves. @done(2026-07-13T00:00:00Z) #git:c943233
- [x] DECIDED the fate of the `design:` frontmatter field (was: dead/unvalidated on 17 posts). Git archaeology (commit d7e50b1 "Add design: back-references to design-capturing posts") revealed the original intent: a plan→design→blog traceability chain, validated by a check-traceability.py that was NEVER committed here. The design docs are REAL and live in the sibling ../earlbear-agentic-workflow/docs/. Decision (user): REVIVE it properly (not drop) — validate via a committed registry file in THIS repo (works without the sibling checked out), with 3 diligence layers matching the audience-split pattern (schema + blocking registry-membership hook + advisory doc-existence check). Implemented as #10. @done(2026-07-13T00:00:00Z) #git:3266ee8
- [x] Revived the `design:` field as enforced plan→design→blog traceability. (a) Built docs/design-registry.json — the contract mapping 13 slugs → their design doc in ../earlbear-agentic-workflow/docs/ (modeled on the sibling repo's own validation-registry pattern). (b) Reconciled the 17 posts: 9 clean, fixed 4 slug↔filename mismatches (live-pitch-deck→pitch-deck, blog-comments→comments, database-approach→database, shared-diagram-engine→diagram-engine), and dropped design: from 2 with no design doc (the-login-page, the-logout-button). (c) FOUND + FIXED a real latent bug: the "sentence-value" on ecommerce-site-scanner-design was never frontmatter at all — it was a fragment of prose inside a <Callout> body ("out of scope for this design: scan-fleet later") that an earlier edit had mangled from a real sentence; restored the prose and added a proper `design: scan-fleet` frontmatter field. (d) Added design: to the zod schema (validated lowercase-kebab slug — build fails on a sentence/typo). (e) Wrote check-design-traceability.py (two-mode: npm run design-check BLOCKS an unregistered slug via PostToolUse exit 2 + CLI; npm run design-docs-check advisorily verifies each registry entry resolves to a real sibling doc, skips if repo absent), wired into package.json, settings.json (PostToolUse, next to the audience guard), and CLAUDE.md (new "Design back-references" section). All 3 layers proven: schema rejects a bad value at build, the hook blocks an unregistered slug (exit 2), docs-check confirms all 13 resolve. Verified: posts-check + design-check + design-docs-check green, both builds clean, audience guard clean, all JSON valid. @done(2026-07-13T00:00:00Z) #git:3266ee8
- [x] Fixed the empty-space bug on the-logout-button post: its "full loop" FlowDiagram used shape="branch" on a CYCLIC graph (edges gate→nav→signout→cleared→regate→gate), which stacked the 5 nodes into a 232x5032 viewBox (21317px rendered → a ~600px empty band). Root-caused in-browser by measuring the SVG viewBox. Fix: shape="loop" (the graph is a loop — the section is literally titled "the full loop"), and dropped the two edge labels ("authenticated"/"unauthenticated") which the loop layout's short arrows can't fit (the engine's label-width gate correctly flagged them; prose already carries that detail). Now renders 605x334. Verified in-browser: proper pentagon loop, no empty band. @done(2026-07-13T00:00:00Z) #git:10ce4d7
- [x] Added a layout-blowout gate to FlowDiagram (blog-side, in FlowDiagram.astro after computeFlowLayout). The engine's existing gates (in the SEPARATE earlbear-design-system repo, symlinked as @earlbear/ui) catch overlaps + clipped labels but NOT a valid-but-insane bounding box — which is how the 5032-tall diagram shipped silently. The gate THROWS if svgH > 4x svgW OR > 2500u. Bounds calibrated against every real diagram in the blog (tallest legit ratio 1.36, tallest ~1014u) so no false positives. Decision (user): aspect-ratio gate only (not a shape-vs-graph topology check), and blog-side (not the design-system engine — no cross-repo commit). Gotcha hit + fixed: Astro's frontmatter parser choked on special chars (the × sign, em-dashes, escaped backticks in the throw string) with a misleading "Unexpected export" error; rewrote with plain ASCII + string concatenation. Proven both ways: build fails with the exact message ("232x5032, height 21.7x the width... branch shape on a graph that is really a loop") when shape=branch is reintroduced, and all 44 pages build clean with the fix. @done(2026-07-13T00:00:00Z) #git:10ce4d7
- [x] Wrote the internal "agent fleet" post series introducing the earlbear-claude-agent fleet on the internal blog. Three new .mdx posts (audience: internal, draft: true, tags [fleet, agents, engineering], author omar): the-agent-fleet.mdx (the HUB — one-codebase-many-agents spine, a FlowDiagram of how work moves Scout→findings→Envoy→Watson, a UseCaseDiagram of who-touches-what, a curated roster with Watson/Scout live + Medic/Envoy as "scaffold, post to follow", and a "how a new agent joins" section), watson-the-overnight-jira-loop.mdx (EARL Kanban loop, column-aware routing, the never-self-approves guardrail; loop-shaped FlowDiagram), and scout-the-storefront-auditor.mdx (queue-driven scan_requests → buy-intent audit → scan_* sink tables → sanitized Drive transcript; pipeline-shaped FlowDiagram, design: scan-fleet). EXTENSIBILITY (per user decision): no new schema/code — a shared `fleet` tag gives a free auto-updating /tags/fleet/ listing, and the hub carries the curated roster; adding an agent later = one new fleet-tagged post + one hub roster line (an MDX comment in the hub documents the extension point). Titles use house voice (no "meet X" framing) per the check-posts voice advisory. Verified: posts-check/diagrams-check/design-check/design-docs-check all green (no advisories on the three new posts); PUBLIC_AUDIENCE=internal build renders all three + /tags/fleet/ (48 pages, diagrams pass the layout-blowout gate); external build correctly EXCLUDES all three posts and the fleet tag page (fail-closed allowlist + draft gate). Sources: ../earlbear-claude-agent (README roster, agents/*/CLAUDE.md, agent.json, architecture/). @done(2026-07-20T00:00:00Z) #git:8ae78c40
- [x] Wrote full per-agent posts for the two scaffold agents (Medic, Envoy) — expanding the fleet series from hub-only scaffold entries to real posts, framed as design-intent since neither is operational yet. Two new .mdx posts (audience: internal, draft: true, tags [fleet, agents, engineering], author omar): medic-the-store-healer.mdx ("the store healer, and why it heals through experiments" — Scout's internal higher-privilege twin routed by is_internal fail-closed Rule 1; heals ONLY through reversible A/B experiments [propose → run A/B → promote-winner/rollback → verify], "the experiment is the guardrail"; the Scout-vs-Medic boundary table, the MEDIC_APPLY apply-gate + dry-run default + medic_changes audit log; loop-shaped FlowDiagram; BUILT=audit/prioritize/propose half + store-healer plugin + medic_* tables, OPEN=deploy half [A/B mechanism, stealth browser/proxy, CAPTCHA/Shopify-write providers]), and envoy-the-outreach-drafter.mdx ("the outreach drafter that never hits send" — turns Scout's public findings into leads: reads scan_* READ-ONLY, finds contact, drafts value-first audit-teardown pitch, files EARL kind:email/kind:pitch-deck lead in Watson's pipeline; the first-class "drafts and queues, NEVER sends" compliance rule [CAN-SPAM/GDPR/CASL — human on the send side]; one-directional Scout→Envoy→Watson handoff; pipeline-shaped FlowDiagram with a human-sends external node; BUILT=skill shape, OPEN=contact-discovery provider, compliance identity ENVOY_FROM_*/ENVOY_PHYSICAL_ADDRESS, outreach_leads funnel/suppression store, no secrets yet). Promoted both in the-agent-fleet.mdx roster (scaffold entries now link the real posts + note what's pending instead of "post to follow"), fixed the hub intro ("each agent has its own post"), and repointed Scout's Envoy/Medic cross-links from the hub to the new posts. Gotcha: Medic's loop FlowDiagram tripped the edge-label-width gate ("promote / rollback" chip 131px > 66px arrow) — shortened the settle-edge label. Verified: posts-check (36) / diagrams-check (33) / design-check / design-docs-check all green, no advisories on the two new posts; PUBLIC_AUDIENCE=internal build renders both + /tags/fleet/ (50 pages); external build EXCLUDES both + the fleet tag page (12 pages). Sources: ../earlbear-claude-agent agents/medic + agents/envoy (agent.json, CLAUDE.md, Envoy SCAFFOLD-NOTES.md). @done(2026-07-20T00:00:00Z) #git:7c53b9b5
